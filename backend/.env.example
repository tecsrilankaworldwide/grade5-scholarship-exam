# MongoDB Configuration
MONGO_URL=mongodb://localhost:27017
DB_NAME_EXAM=exam_bureau_db

# Security (CHANGE IN PRODUCTION!)
# Generate with: openssl rand -hex 32
SECRET_KEY=your-secret-key-change-this-in-production-min-32-chars

# CORS Configuration (comma-separated origins)
CORS_ORIGINS=http://localhost:3000,https://educationreforms.cloud

# Optional: File Upload Directory
# UPLOAD_DIR=/app/backend/uploads/exam_pdfs

# Optional: Service Credentials (if using external services)
# PAYPAL_CLIENT_ID=
# PAYPAL_CLIENT_SECRET=
# PAYPAL_MODE=sandbox
# TWILIO_ACCOUNT_SID=
# TWILIO_AUTH_TOKEN=
# TWILIO_WHATSAPP_NUMBER=whatsapp:+14155238886
